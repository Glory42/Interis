import { Card, CardContent } from "@/components/ui/card";

export const CommentThread = () => (
  <Card>
    <CardContent className="p-4 text-sm text-muted-foreground">
      Comment thread component is reserved for Phase 2 social features.
    </CardContent>
  </Card>
);
