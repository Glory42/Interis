import { Card, CardContent } from "@/components/ui/card";

export const ListCard = () => (
  <Card>
    <CardContent className="p-4 text-sm text-muted-foreground">
      List creation and ranking UI is planned for Phase 3.
    </CardContent>
  </Card>
);
