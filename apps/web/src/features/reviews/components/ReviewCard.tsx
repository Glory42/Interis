import { Card, CardContent } from "@/components/ui/card";

export const ReviewCard = () => (
  <Card>
    <CardContent className="p-4 text-sm text-muted-foreground">
      Review UI lands in Phase 2 with spoiler flags, comments, and likes.
    </CardContent>
  </Card>
);
