-- No-op: every statement in this migration duplicated
-- 0021_interaction_ratings.sql (same columns, just reordered) - a
-- snapshot-drift artifact from generation. Removed to let migrations replay
-- cleanly against a fresh database.
SELECT 1;
