-- No-op: every statement in this migration duplicated
-- 0013_serial_detail_activity.sql (same tables, same constraints, just
-- reordered) - a snapshot-drift artifact from generation. Removed to let
-- migrations replay cleanly against a fresh database.
SELECT 1;
